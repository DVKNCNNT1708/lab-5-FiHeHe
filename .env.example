# API configuration
APP_HOST=0.0.0.0
APP_PORT=8000
AUTH_TOKEN=local-dev-token
SERVICE_NAME=iot-ingestion
SERVICE_VERSION=0.5.0
ENV=local

# Database configuration
# QUAN TRỌNG: Dùng tên service "db" (KHÔNG dùng localhost)
POSTGRES_USER=lab05
POSTGRES_PASSWORD=lab05pass
POSTGRES_DB=iotdb
DATABASE_URL=postgresql://lab05:lab05pass@db:5432/iotdb

# AI service configuration
# QUAN TRỌNG: Dùng tên service "ai-service" (KHÔNG dùng localhost)
AI_SERVICE_URL=http://ai-service:9000
AI_SERVICE_NAME=ai-service
AI_SERVICE_VERSION=0.1.0